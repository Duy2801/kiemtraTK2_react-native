export interface Products{
    id: number;
    name: string;
    price: number;
    unit: string;
    description: string;
    img_uri: string;
    category: string;
}
export interface Categorys{
    id: number;
    name: string;
}